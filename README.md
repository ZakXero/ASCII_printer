# ASCII CHARS PRINTER BANNER
Print ASCII chars in format banner.

# Installation

```bash
# Create environment
python3 -venv venv
# Install packet in python
python3 pip -install ASCIIPrinter

```

# Usage
```python
ascii
```